# 20-Hour Hands-Off Sprint — control + live log

**Started:** 2026-06-03 · **Window:** ~20h · **Founder:** finnduns@gmail.com
**Driver:** in-session orchestrator (Opus) + `/loop`; all state committed here so any
fire (session or cloud) resumes from this doc. **This doc is the single source of
truth for the sprint** — kept separate from `pre-launch-wave-status.md` to avoid its
single-owner write race.

## Locked dials (founder-chosen, 2026-06-03)
- **Merge ceiling:** auto-merge **Tier A + B + C** (C after a 30-min quiet window),
  straight to `main` → Netlify live mirror.
- **Mission:** **fix + build-heavy** — bots ("real users") drive bug-squashing; the
  loop drains `BUILD-EVERYTHING-QUEUE.md` + `REMEDIATION_QUEUE.md`.
- **Merge gate (infra-adapted — "I'm the gate"):** every change gets a full local
  `tsc + test + lint`; merge when that is green **and** the PR's `ci.yml`
  (`Lint·Type-check·Test·Build` + the compliance/RLS/secret gates) is green.
  **Explicitly ignore the 3 known-infra reds** — they are environmental, not the
  diff's fault:
  - `Vercel` — account billing-blocked (only the founder can clear).
  - `Preview smoke test` — depends on the blocked Vercel deploy.
  - `Supabase types drift` — standing live-schema-vs-committed-types baseline.
- **Bot target:** **Netlify** live mirror `lambent-sawine-17c3dd.netlify.app`
  (+ per-PR previews) — Netlify works; only Vercel is down.

## Hard lines — NEVER autonomous (build only as held draft PRs for founder + legal)
- **Avoid-list escalators** (`docs/strategy/REGULATORY-AVOID-LIST.md`): money-movement
  / payment clips, %-of-advice-fee, personal advice, credit assistance, capital-raise
  facilitation, CDR bank-data, AML, custody / client-money, product-issuing.
- **Tier D** (`do-not-merge` / "set env var first" / "DO NOT APPLY this migration").
- **Tier E** (force-push, branch delete, repo / workflow settings, anything `git
  revert` can't undo).
- **Founder-authored PRs.** (Agent-authored loop PRs — branch prefixes `claude/*`,
  `pre-launch/*`, `fix/*` + "Generated by Claude Code" — are in scope.)
- Every candidate is checked against the avoid-list **first**; only if it clears does
  the tier policy apply.

## Cycle protocol (each fire)
1. **Pre-flight:** `git fetch origin main`; confirm main `ci.yml` is green; read this
   doc + the two queues.
2. **Discover:** rotate — a bot pass (`/ai-journey` vs Netlify, real-user personas)
   and/or `/audit-remediation-scout`. Verify every finding with retries (the proxy
   throws transient 403/503) → append to the relevant queue.
3. **Build / fix:** drain the queue — build-heavy from `BUILD-EVERYTHING-QUEUE`,
   fixes from `REMEDIATION_QUEUE`. One mergeable chunk, or up to 3 via parallel
   worktree agents.
4. **Verify (local):** `tsc --noEmit <changed>` + `vitest run <changed/related>` +
   `eslint <changed>`. Red → fix or drop. Never merge a red iteration.
5. **Merge:** open PR; when local-green **and** `ci.yml` green → merge via API per
   tier (A/B immediately, C after a 30-min quiet window). Ignore the 3 infra reds.
6. **Verify (bots):** next cycle, re-run the touched journey vs the freshly-deployed
   Netlify mirror to confirm the fix landed.
7. **Log:** append a dated line to the Live log below. The auto-revert workflow
   backstops any bad merge (~5 min).

## Stop conditions
Queue drained (`LOOP_DONE` sentinel) · usage floor reached · an avoid-list / Tier-E
item needs the founder · founder says stop.

## Durability note
A self-paced in-session `/loop` runs as long as this session lives. For guaranteed
survival across a disconnect, the same protocol can run as a cloud `/schedule` (cron,
survives terminal close) — state lives here, so either driver resumes identically.

---

## Live log
- **2026-06-03 ~04:35 — Cycle 0 (pre-flight + foundation):**
  - main is **GREEN** — `ci.yml` `success` on `93e6d36e` (06-02). The cron-test red
    that older PRs (#1265/#1268) reported was fixed upstream by **#1278**; all 4 cron
    tests pass locally (26/26). **Phase 0 (green main) already complete — no rescue
    needed.**
  - **Infra:** `Vercel` account billing-blocked (sitewide deploys/previews down; only
    founder can clear) → bots will use the **Netlify** mirror. Auto-merge automation
    is off since **#1197** (founder switched to manual-only) → I merge via API.
  - **Backlog triage (open agent PRs):** ready-ish → **#1269** (autopilot cron gate,
    Tier B, 62 tests), **#1198** (dup-fn allowlist, Tier A, small). Hold → **#1272**
    (carries "DO NOT APPLY" migrations = Tier D), **#1180** (`needs-human-review`,
    Tier C SMS-consent), **#1271 / #1273 / #1274** (large drafts — triage per item).
  - Control doc created; sprint loop armed.
- **2026-06-03 ~04:45 — Cycle 1 (build-everything fire → finding):** the build queue
  has **0 unbuilt `[ ]` items** — 43 are `[~]`, 7 `[x]`, 7 `[!]` blocked. **CORRECTION
  (founder caught it):** the 43 `[~]` markers are **stale bookkeeping**, NOT 43 open
  PRs — most of that work already merged via the big batch merges and the checkboxes
  were never flipped. The **actual open-PR count is 13** (matches GitHub). Earlier
  "40+ backlog" was wrong. Real substantive agent-built backlog ≈ 8 PRs; the rest are
  mine (#1305/#1306), superseded (#1268, fixed by #1278), or automated (#1265/#1276).
  **Recalibration:** loop pivots to (a) **bots find NEW issues** (`/ai-journey` vs
  Netlify) → I build + merge those (additive, safe, on-brief); (b) **selective** merge
  of only clearly-safe small Tier-A/B PRs (#1198, #1269) after local re-verify; close
  the superseded #1268; (c) the big drafts (#1271/#1272/#1273/#1274) + held
  (#1180/#1159 needs-review, #1272 "DO NOT APPLY" migrations) **stay for the founder**.
  Also: reconcile the stale `[~]` queue markers. Sprint home PR: **#1306**.
- **2026-06-03 ~05:00 — Cycle 2 (infra reality → the loop has little autonomous
  fuel):**
  - **No reachable live site.** Vercel billing-blocked **and** the Netlify mirror
    (`lambent-sawine-17c3dd.netlify.app`) times out (HTTP 000). Egress itself works
    (google/example.com 200). No local env either (only `.env.local.example`). →
    **live-bots can't run** — the headline premise is infra-blocked.
  - **Build-Everything queue: 0 unbuilt items.** **Remediation queue: streams A–N all
    complete**; everything remaining sits in "Blocked — needs human input" (CDR
    accreditation, compliance signoffs, registrar/DNS, GSC/GA4 creds, Vercel billing).
  - **Conclusion:** the autonomous build/fix fuel is nearly drained — not a failure,
    the platform is **pre-launch-complete and waiting on founder/legal gates** (which
    the loop is explicitly *not* allowed to action).
  - **Actions taken:** closed superseded **#1268** (backlog 13→12).
  - **Highest-leverage unblock (founder, ~5 min):** get one deploy live (Vercel
    billing OR a working Netlify build) → turns the live-bots vision back on. Until
    then the loop's real options are: stand up **local-bots** (run the app in-sandbox)
    or clear the **safe backlog** (#1198/#1269) + reconcile bookkeeping.
- **2026-06-03 ~05:30–06:35 — Cycles 3–6 (Netlify came back; bots + fixes shipping):**
  - **Netlify recovered** (root/`/invest`/`/best` → 200) → **live-bots unblocked**;
    chromium found at `/opt/pw-browsers` (env-redirected). Local-bots now moot.
  - **Merged to main:** **#1269** (autopilot cron gate, Tier B) ✅, **#1274** (GEO
    AI-traffic measurement) ✅ — both verified locally (tsc + full suites) then merged
    via API on core-gate-green (ignoring the 3 infra reds, as designed).
  - **Backlog grind:** **#1271** (15-wave hardening, ~950 tests) — rebased onto main
    (12 conflicts resolved toward main's correct versions), full suite **16,776 tests**
    green; CI flagged its `no-unvalidated-req-json` error-promotion (37 pre-existing
    main violations) → reverted the severity bump to `warn`, re-pushed. **#1272/#1273
    HELD** (carry DB migrations).
  - **Bots (live Netlify):** 4 personas. `/invest` → **5 listing-detail 500s**
    (`funds` + `digital-infrastructure` verticals — 11 verticals lack a `listings/[slug]`
    route; likely a stale-deploy 500 since the code path should 404). **Documented for
    founder, NOT auto-fixed** — architectural + the wholesale verticals are avoid-list
    (C8 wants them gated/unpublished). `/best`, `/compare`, `/advisors` → **healthy**
    (the `/advisors/financial-planners` 403 was transient proxy noise — 200×4 on retry).
  - **3 parallel review agents → real, untracked bugs fixed (each verified + PR'd):**
    - **#1307** — 7 dead auth-link 404s (`/sign-in`, `/auth/sign-up`, `/login?redirect=`
      → `/auth/login?next=` / `/auth/signup`) + next.config redirect net. (CI caught a
      test asserting an old URL → fixed the assertion.)
    - **#1308** — `clubs/[clubId]/messages` POST 500 on every send: `withValidatedBody`
      dropped Next's route ctx → fixed at the wrapper (forwards ctx; hardens ~47 routes)
      + added the missing test.
    - **#1309** — get-matched top-match scoring silently zeroed (quiz_weights `*_weight`
      columns vs unsuffixed `WeightKey`s) → typed remap helper + regression test.
  - **HELD for founder:** `advisor-credit-ledger` optimistic-lock retry uses a stale
    balance (Tier C / financial) — surfaced, not auto-fixed.
  - **Process note:** local `tsc+lint` missed 2 CI failures (full suite + build catch
    more) → now running related/full tests before pushing.
  - **In CI, verified, auto-merge on green:** #1271, #1307, #1308, #1309.
